<!DOCTYPE html>
//- view for pulling up googlemaps api streetview with map modal on top
html
  head
    style 
      include styles.css
    title Google Maps Integration


  body
    #pano(style="position: absolute; width: 100%; z-index: -1;")

    //- Modal HTML
    #mapModal.modal(style="position: relative;")
      .modal-content
        span.close &times;
        #map(style="position: fixed; width: 100%; margin: 0; padding: 10px; z-index: 2")

    //- Trigger Modal Open Button
    button#openModalButton Open Map Modal

    //- Load Google Maps API dynamically (handled in maps.js)
    script(src="/create.js")
